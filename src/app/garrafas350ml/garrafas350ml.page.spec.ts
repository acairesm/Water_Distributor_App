import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Garrafas350mlPage } from './garrafas350ml.page';

describe('Garrafas350mlPage', () => {
  let component: Garrafas350mlPage;
  let fixture: ComponentFixture<Garrafas350mlPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Garrafas350mlPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
